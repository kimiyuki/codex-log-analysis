# Markdown Export Nice To Have

- 1 セッションから summary note / full transcript note の複数ノートを切れるようにする
- `state_*.sqlite` の `archived` や `first_user_message` も frontmatter に反映する
- `function_call_output` を解釈して command / patch / plan / file change に分類する
- `compacted.replacement_history` を読み、compact 前の重要会話を Notes に要約して残す
- 期間指定や archived 併読を CLI オプションで素直に扱えるようにする
- fixture ベースの `unittest` を追加し、`full` / `filtered` の除外規則を固定する
